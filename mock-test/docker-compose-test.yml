version: '3.8'

x-mock-base: &mock-base
  build:
    context: .
    dockerfile: Dockerfile
    additional_contexts:
      - agent=../spring-watch-agent
      - sdk=../spring-watch-sdk
  image: mock-test:1.0.0
  environment:
    - TZ=Asia/Shanghai
    - JAVA_OPTS=-Xmx256m
  extra_hosts:
    - "host.docker.internal:host-gateway"
  restart: unless-stopped
  healthcheck:
    test: ["CMD", "curl", "-f", "http://localhost:8081/api/ping"]
    interval: 10s
    timeout: 5s
    retries: 3
    start_period: 30s

services:
  mock-test-1:
    <<: *mock-base
    container_name: mock-test-1
    ports:
      - "18081:8081"
      - "19464:9464"
    environment:
      - SW_APP_NAME=mock-test-1
      - SW_METRICS_PORT=9464
      - SERVER_PORT=8081

  mock-test-2:
    <<: *mock-base
    container_name: mock-test-2
    ports:
      - "18082:8082"
      - "19465:9465"
    environment:
      - SW_APP_NAME=mock-test-2
      - SW_METRICS_PORT=9465
      - SERVER_PORT=8082

  mock-test-3:
    <<: *mock-base
    container_name: mock-test-3
    ports:
      - "18083:8083"
      - "19466:9466"
    environment:
      - SW_APP_NAME=mock-test-3
      - SW_METRICS_PORT=9466
      - SERVER_PORT=8083
