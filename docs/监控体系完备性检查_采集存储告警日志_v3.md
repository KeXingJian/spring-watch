

## 三、采集层(Collector + Consumer + Schedule + Ingest)

### 3.1 已实现(实际代码,本版 v2 几乎一致)

| 模块 | 类 | 状态 |
|---|---|---|
| 注册表 | `MonitorAppService` + `MonitorAppRepository` | ✅ 完整 |
| 调度 | `CollectScheduleRegistry` | ✅ 完整 |
| 限流 | `HostThrottler` + `PullRetryQueue` | ✅ 完整 |
| 心跳 | `HeartbeatEvent` + `BatchHeartbeatConsumer` | ✅ 完整 |
| 指标采集 | `AgentMetricsCollector` | ✅ 基础 |
| 日志采集 | `AgentLogCollector` | ⚠️ **v1 补丁**(白皮书 §10 / Q0.5) |
| 重投 | `PullRetryQueue` | ✅ 完整 |
| Kafka 降级 | `KafkaFallbackQueue` | ✅ 完整 |
| 摄入 | `LogParser` + `LogFingerprinter` + `LogSanitizer` + `LogDedupService` | ✅ 完整 |
| Topic 自建 | `KafkaTopicConfig` | ✅ 完整 |

### 3.2 缺失清单(本版 P0 重排,**已剔除 v1 已知补丁**)

#### P0 - 阻断性(v1/v2 都必须做)

| # | 缺失 | 现状(代码定位) | 影响 |
|---|---|---|---|
| 1 | **HTTP 连接池缺失** | `AgentMetricsCollector.java:28-32`、`AgentLogCollector.java:31-34`、`AppPullTask.java:134-138` **3 处** 都是 `new HttpURLConnection`,无池化 | 高并发拉取握手开销大、TIME_WAIT 堆积 |
| 2 | **PullRetryQueue 无容量上限** | `PullRetryQueue.java:33-34` `PriorityBlockingQueue<>(64, ...)` 初始 64,**无界** | agent 全挂时 retry 任务无限堆,OOM |
| 3 | **InfluxDB 无 retention policy** | `InfluxDBBucketInitializer.java:50-51` `createBucket()` **只建 bucket,没设 retentionSeconds** | **最致命**,生产 1 个月撑爆磁盘 |
| 4 | **InfluxDB 无 downsample** | `InfluxDBBucketInitializer` 不创建 Task | 高频指标成本失控 |
| 5 | **PostgreSQL 缺关键索引** | `alert_history` 缺 `(appid, created_at DESC)` 复合索引;`monitor_app` 缺 `(status, last_heartbeat)` | 看板"最近告警"全表扫 |
| 6 | **InfluxDB 高基数 tag** | `BatchLogConsumer.java:110` 把 `fingerprint` 放 tag,sha1Hex 理论基数无限 | TSI 索引爆,严重 OOM |
| 7 | **AlertRuleService.createRule 缺字段** | `AlertRuleService.java:29-31` 签名不接 `level/times/template`,V6/V7/V8 列写不进去 | 用户加 level=CRITICAL 规则只能手工 SQL |
| 8 | **SMTP 部署链路断裂** | `application.yml:60` `${MAIL_AUTH_CODE}`,`docker-compose.yml` 未注入 | 容器化部署时邮件 100% 失败 |

#### P1 - 短板

| # | 缺失 | 现状 | 影响 |
|---|---|---|---|
| 12 | agent 失活无独立探测线程 | `isReachable()` 随 `AppPullTask` 走,最长 15s+ 才感知;`lastHeartbeat` 写但**无超时对比定时器** | 主动心跳超时告警缺失 |
| 13 | 采集层无 Micrometer 自监控埋点 | 无 `MeterRegistry / Counter / Timer` | 平台自监控全靠日志 |
| 15 | InfluxDBClient 不池化 + 无 close | `InfluxDBClientFactory.create()` 每次注入(实际 1 次),`@PreDestroy` 无 close | 关闭后连接悬挂 |
| 16 | BatchLogConsumer 写失败整批回滚 | `BatchLogConsumer.java:93` `throw new RuntimeException(e)` | 1 条格式错死循环 |
| 17 | InfluxDB queryApi 不池化 | `LogAggregator.java:48/97/140` 每次 `getQueryApi()` | 高并发查询连接堆积 |
| 19 | DLQ 消息未落库 | 无 `dlq_message` 表 | DLQ 消息无法查询/重投 |
| 20 | `@ConditionalOnProperty` 全缺失 | 全代码 0 命中 | 无法临时关告警 |


---

## 四、存储层

> 几乎与 v2 一致,本版不重复展开(详见 `监控体系完备性检查_采集存储告警日志_v2.md` §四)。

| 关键变化 | 内容 |
|---|---|
| P0 仍保留 | retention、downsample、复合索引、高基数 tag、DLQ 落库(同 v2) |
| P1 仍保留 | metric_dict 死代码、InfluxDBClient 无 close、queryApi 不池化、BatchLogConsumer 写失败回滚(同 v2) |

---

## 五、告警层

> 几乎与 v2 一致,本版不重复展开。

| 关键变化 | 内容 |
|---|---|
| P0 仍保留 | AlertRuleService 缺字段、AlertController 不存在、SMTP 部署链路、AlertNotifier 仅 SMTP、@ConditionalOnProperty(同 v2) |
| P1 仍保留 | log_keyword 走偏、告警抑制、告警去重/合并、模板无 if/for、恢复无节流(同 v2) |

---

## 六、日志分析层

> 几乎与 v2 一致,本版不重复展开。

| 关键变化 | 内容 |
|---|---|
| P0 仍保留 | dedup 计数未双写、LogQueryService search 慢、无上下文查询(同 v2) |
| P1 仍保留 | 多维聚合、时序对比、季节性、业务化(同 v2) |

**特别说明**:`AgentLogCollector` 拉取 `/api/agent/logs` 是 v1 已知补丁(白皮书 Q0.5),**不计入 P0 缺口**,纳入 §十一 已知补丁清单。

---

## 七、跨四模块"非功能性"缺口

> 几乎与 v2 一致,本版不重复展开。

---

## 八、按优先级分层的总待办清单(本版重排)

### P0 - 阻断产品主流程 + 阻断白皮书(v1/v2 都必须做)

| # | 任务 | 模块 | 涉及文件 | 工时 | 修正 v2 |
|---|---|---|---|---|---|
| 1 | 补 `MonitorAppController` 全部方法 | web | `MonitorAppController.java` | 1d | 沿用 v2 P0-2 |
| 2 | 补 `MetricController` 全部方法 | web | `MetricController.java` | 0.5d | 沿用 v2 P0-3 |
| 3 | `AlertController` 新建 | web | 新建 | 1d | 沿用 v2 P0-4 |
| 4 | `AlertRuleService.createRule` 补 `level/times/template` 字段 | service | `AlertRuleService.java:29-31` | 0.5h | 沿用 v2 P0-5 |
| 5 | `docker-compose.yml` 注入 `MAIL_AUTH_CODE` | deploy | `docker-compose.yml` | 0.5h | 沿用 v2 P0-6 |
| 6 | InfluxDB retention policy | 存储 | `InfluxDBBucketInitializer` | 0.5d | 沿用 v2 P0-7 |
| 7 | InfluxDB downsample 任务 | 存储 | 新建 Task | 0.5d | 沿用 v2 P0-8 |
| 8 | PostgreSQL 加复合索引 | 存储 | V12 migration | 0.5h | 沿用 v2 P0-9 |
| 9 | InfluxDB 高基数 tag 防护(`fingerprint` 移到 field) | 存储 | `BatchLogConsumer.java:110` | 0.5d | 沿用 v2 P0-10 |
| 10 | HTTP 连接池替换 | 采集 | 3 个 Collector | 0.5d | 沿用 v2 P0-11 |
| 11 | `PullRetryQueue` 容量上限 | 采集 | `PullRetryQueue.java` | 0.5h | 沿用 v2 P0-12 |
| 12 | dedup 计数双写 + 风暴样本落库 | 日志 | `LogDedupService` + `BatchLogConsumer` | 1d | 沿用 v2 P0-13 |
| 13 | Webhook/钉钉/飞书 通知 | 告警 | `AlertNotifier` | 1d | 沿用 v2 P0-14 |
| 14 | `@ConditionalOnProperty` 全局开关 | 告警 | `AsyncAlertExecutor` / `PendingStateScanner` | 0.5h | 沿用 v2 P0-15 |
| ~~15~~ | ~~解除 `SpringWatchAspect` 注释~~ | ~~annotation~~ | — | — | **v2 P0-1 取消,纳入 §十一 v1 已知补丁** |

> **P0 完成后**(v3 重排):
> - v1 现状:四大模块从"能跑"升级为"生产可用",**白皮书 5 大硬约束全部合规**
> - v2 目标:无需任何额外工作,自研 Agent 上去即可

### P1 - 补齐生产短板(本版几乎与 v2 一致,详见 v2 §八 P1)

### P2 - 高级能力(本版几乎与 v2 一致,详见 v2 §八 P2)

---

## 九、四大模块完成度评分(本版重评)

| 模块 | 链路完整 | 生产可用 | 扩展能力 | 白皮书 v1 合规 | 白皮书 v2 合规 | 综合 |
|---|:---:|:---:|:---:|:---:|:---:|:---:|
| 采集 | ⭐⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐ | ✅ | ✅ | **70%** |
| 存储 | ⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐ | ✅ | ✅ | **60%** |
| 告警 | ⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐ | ✅ | ✅ | **65%** |
| 日志分析 | ⭐⭐⭐⭐ | ⭐⭐ | ⭐⭐ | ✅ | ✅ | **55%** |
| **白皮书合规** | — | — | — | **70%** | **40%** | — |

**v2 → v3 评分变化**:
- 采集:55% → **70%**(+15,v1 过渡方案合法,合规扣分取消)
- 存储:60% → 60%(不变)
- 告警:65% → 65%(不变)
- 日志分析:55% → 55%(不变)
- 白皮书 v1 合规:40% → **70%**(+30,v1 过渡方案明确)
- 白皮书 v2 合规:**40%**(自研 Agent 立项尚未启动)

> 当前离"v1 生产可用"差距最大的是: **存储(retention/索引)** 和 **Controller 接入层**;
> 当前离"v2 终态"差距最大的是: **自研 `spring-watch-agent.jar` 立项与实现**。

---

## 十、v2 → v3 关键修正(本版相对 v2)

| v2 # | v2 原文 | v3 修正 |
|---|---|---|
| §〇 / §一 | "SpringWatchAspect 注释 100% 不成立,白皮书约束 5 100% 不成立" | ❌ **v3 修正**:白皮书 v1.1 §10 + Q0 已明确"v1 复制 2 文件是过渡",**v1 现状合规,只是有胶水代价**;v2 目标才真正零侵入 |
| §一 C5 | "❌ 明确违规" | ⚠️ **v3 修正**:"v1 合规但有胶水代价 / v2 真正合规" |
| §三 P0-1 | "`SpringWatchAspect.java` 整文件被注释,P0 最高优先级" | ✅ **v3 降级**:移入 §十一 v1 已知补丁,**不再算 P0** |
| §一 C3 | "潜在违规" | ✅ **v3 修正**:"v1 合规,无问题" |
| §四 P0-9(已删) | "dedup 计数未双写" | ✅ 仍为 P0(本版 §四 P0-12) |
| §八 P0-15 | "解除 SpringWatchAspect 注释" | ✅ **v3 取消** |

---

## 十一、v1 已知补丁清单(本版新增,与 P0/P1/P2 并列)

> 白皮书 v1.1 §10 演进路线 + Q0 / Q0.5 已明确承认以下是 v1 过渡方案,**不需要现在修**,纳入 v2 自研 agent 路线图。

| # | 补丁项 | v1 现状 | v2 目标(自研 agent) | 影响范围 |
|---|---|---|---|---|
| 1 | **`SpringWatchAspect.java` 整文件注释** | OTel 不读 `@SpringWatch`,客户需复制 AOP 切面 | 自研 Agent 字节码读注解织入,无需复制 | 客户接入流程(6 步) |
| 2 | **`SpringWatch.java` 注解需客户复制** | 客户项目 `com.springwatch` 包内 | 自研 Agent 自带,无需 import | 客户接入流程(6 步) |
| 3 | **`spring-boot-starter-aop` 依赖需加** | 客户 pom.xml | 不需要 | 客户 pom |
| 4 | **`@SpringBootApplication` 加 `scanBasePackages`** | 1 行注解改动 | 不需要 | 客户启动类 |
| 5 | **`/api/agent/logs?since=` 日志端点** | 客户内嵌 `InMemoryLogBufferAppender` + `AgentLogController` | 自研 Agent 暴露统一日志端点 | 客户应用 |
| 6 | **`spring-watch-log-pull-extension.jar`** | 客户启动时挂载(在 OTel 之外额外加) | 整合进 `spring-watch-agent.jar` | 客户启动 |
| 7 | **OTel Java Agent 全量埋点** | 客户启动时挂载 `opentelemetry-javaagent.jar` | 卸载 OTel,改挂自研 Agent | 客户启动 |
| 8 | **方法调用栈进入 spring-watch AOP 类** | AOP 切面在客户代码里 | 字节码完全在客户代码内,调用栈不可见 | 客户 debug 体验 |

**平台侧影响**:**0 改动**。v1 → v2 迁移时,`AgentLogCollector` 的拉取 URL 由自研 Agent 决定(可能仍是 `/api/agent/logs?since=`,也可能换名),**只要是 GET 拉取,平台侧 0 改动**。

---

## 十二、v1 → v2 迁移影响面(本版新增)

| 主体 | 迁移内容 | 工时 | 备注 |
|---|---|---|---|
| **客户应用** | 删除 2 个文件 + 改 pom + 改启动注解 + 改 `-javaagent:` 参数 | 10 分钟/应用 | 见白皮书 §10.4 |
| **自研 Agent 仓库** | 新建仓库 + 字节码织入 + `/metrics` exporter + 日志 hook + 日志 exporter + 配置注入 | 数月 | 不在 spring-watch 主仓库 |
| **平台侧(`spring-watch` 主仓库)** | **0 改动** | 0 | `AgentLogCollector` URL 改动由 Agent 决定,平台只接 JSON |
| **`mock-test` 模块** | 删除 `SpringWatchAspect.java` / `InMemoryLogBufferAppender.java` / `AgentLogController.java`,改挂 `spring-watch-agent.jar` | 半小时 | mock-test 改为 v2 接入示例 |
| **白皮书** | 接入流程 §6 大幅简化,Q0/Q0.5 转为历史脚注或删除 | — | 见白皮书 §10.3 |
| **本报告** | 删除 §十一 v1 已知补丁,白皮书 v1/v2 合规评分合并 | — | 见报告 v4 |

---

## 十三、版本控制

- 本报告变更需经架构师评审
- 任何 v3 编号以上的更新,在 PR 描述中说明修正了 v3 哪几条
- v3 → v4 升级条件:
  - P0 全部完成
  - **或**自研 `spring-watch-agent.jar` 立项启动(转入 v2 评估维度)
